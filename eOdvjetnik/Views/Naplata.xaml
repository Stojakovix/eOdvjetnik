<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="eOdvjetnik.Views.Naplata"
             xmlns:viewmodel="clr-namespace:eOdvjetnik.ViewModel"
             Title="Naplata">

    <ContentPage.BindingContext>
        <viewmodel:NaplataViewModel/>
    </ContentPage.BindingContext>

    <VerticalStackLayout >
        <Button 
                BorderColor="DarkGray"
                BorderWidth="1"
                BackgroundColor="#5ac8fa"
                Text="Izradi račun"
                FontAttributes="Bold"
                TextColor="Black"
                HorizontalOptions="Start"
                Clicked="CreateDocument"
            FontSize="Default"
            Margin="10,10,10,10"
            />

        <Label Text="Pretraga prema tarifnom broju" HorizontalOptions="Start" FontSize="Default"  Margin="10,10,10,10"
 />
        <SearchBar Placeholder="Npr. 1.2" 
                   BackgroundColor="LightGray"
                   SearchCommand="{Binding SearchCommand}"
                   Text="{Binding SearchText}"
                   WidthRequest="400"
                   HorizontalOptions="Start"
                   Margin="10,0,10,10"
                   FontSize="Default" />


        <Grid Margin="0,0,10,0" Padding="10,10,10,10" >
            <Grid.Resources>
                <Style TargetType="Label">
                    <Setter Property="TextColor" Value="Black" />
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <HorizontalStackLayout Grid.Column="0">
                <Label FontSize="Default"  Text="Broj bodova:" HorizontalOptions="Start" Padding="5,5,5,5" />
                <Label Grid.Column="1" FontSize="Default" Text="Vrsta spisa:" HorizontalOptions="Start" Padding="5,5,5,5" />
                </HorizontalStackLayout >

        </Grid>
        <ListView ItemsSource="{Binding TariffItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Margin="5" Padding="5" BackgroundColor="White">
                            <Grid.Resources>
                                <Style TargetType="Label">
                                    <Setter Property="TextColor" Value="Black" />
                                </Style>
                            </Grid.Resources>
                            
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <Label Grid.Row="1" Grid.Column="0" Text="{Binding bodovi}" HorizontalOptions="Center" />
                            <Label Grid.Row="1" Grid.Column="1" Text="{Binding name}" HorizontalOptions="Start"  />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>

</ContentPage> 






<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:eOdvjetnik.Models"
             xmlns:data="clr-namespace:eOdvjetnik.Data"
             x:Class="eOdvjetnik.Views.Dokumenti"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="True"
             Title="Dokumenti" >



    <ScrollView>
        <VerticalStackLayout>


    <Grid RowDefinitions="*,Auto">

        <!--<CollectionView
            
            VerticalOptions="Fill"
            ItemsSource="{Binding Items}"
            SelectionMode="Single"
            SelectionChanged="CollectionView_SelectionChanged">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:DocsItem">
                    <Grid ColumnDefinitions="*, Auto" Padding="20">
                        <Image Source="e_odvjetnik.png"/>
                        <Label Text="{Binding Name}"
                               VerticalTextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>-->

                <ListView ItemsSource="{Binding ShareFiles}"
          Margin="20">
                    <ListView.ItemTemplate>
                      <DataTemplate>
                        <ViewCell>
                          <StackLayout Orientation="Horizontal" Padding="2" >
                            <Image Source="folder_1484.png" WidthRequest="50" HeightRequest="50" Aspect="Fill"/>
                                    <Label x:Name="DocumentLabel" Text="{Binding .}" VerticalTextAlignment="Center" Margin="10,0,0,0" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" TextColor="Black">

                            </Label>
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnLabelTapped" NumberOfTapsRequired="1"/>
                                    </StackLayout.GestureRecognizers>
                          </StackLayout>
                        </ViewCell>
                      </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>






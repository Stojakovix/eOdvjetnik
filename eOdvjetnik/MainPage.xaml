<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="eOdvjetnik.MainPage">

   
    <ScrollView>
        <VerticalStackLayout>

            <!-->
            NAS FORMS
            -->

<StackLayout  x:Name="NASForm" IsVisible="false">
    <Label Text="IP adresa" />
    <Entry x:Name="IPEntry" />

    <Label Text="Korisničko ime" />
    <Entry x:Name="USEREntry" />

    <Label Text="Lozinka" />
    <Entry x:Name="PASSEntry" />


    <Button Text="Save" Clicked="OnSaveClicked" />
    <Button Text="Load" Clicked="OnLoadClicked" />
    <Button Text="Delete" Clicked="OnDeleteClicked" />

</StackLayout>
            <StackLayout  x:Name="MySQLForm" IsVisible="false">
    <Label Text="IP adresa" />
    <Entry x:Name="IPEntryMySQL" />

    <Label Text="Korisničko ime" />
    <Entry x:Name="USEREntryMySQL" />

    <Label Text="Lozinka" />
    <Entry x:Name="PASSEntryMySQL" />

    <Label Text="Ime baze podataka" />
    <Entry x:Name="databasenameEntryMySQL" />

    <Button Text="Save" Clicked="OnSaveClickedMySQL" />
    <Button Text="Load" Clicked="OnLoadClickedMySQL" />
    <Button Text="Delete" Clicked="OnDeleteClickedMySQL" />

</StackLayout>
<Button Text="NAS Postavke" Clicked="NASPostavkeClicked" />
<Button Text="MySQL Postavke" Clicked="MySQLPostavkeClicked" />


            <!-->
            NAS FORMS
            -->
            <Image
                Source="folder_1484.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" 
                BackgroundColor="White"
                />
            <Label
                Text="Dobrodošli"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />
            
            <Label
                Text="VPN server"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="NAS server"
                FontSize="18"
                HorizontalOptions="Center" />
            <Button
                x:Name="CounterBtn"
                StyleClass="my-button"
                Text="Spoji se"
                SemanticProperties.Hint="Spoji se"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center" >
                <!--<Button.Style>  !XML NAČIN DODAVANJA STILA!
                    <Style TargetType="Button">
                        <Setter Property="BackgroundColor" Value="#007acc" />
                        <Setter Property="TextColor" Value="White" />
                        
                        <Setter Property="Padding" Value="10, 20" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="FontAttributes" Value="Bold" />
                        
                        <Setter Property="TextTransform" Value="Uppercase" />
                    </Style>
                </Button.Style>-->
            </Button>
        </VerticalStackLayout>
        
    </ScrollView>
    
</ContentPage>

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="eOdvjetnik.MainPage"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:local ="clr-namespace:eOdvjetnik.ViewModel"
             >

<!--Crasha se CSS kad implementiraš ->
    font-size: 15px;
                    left: 1;
                width: 99%;
padding-right: 30;
    border-radius: 5;
    padding: 5 5;
    min-height: 65;
    height: 100%;
    -->
    <ContentPage.Resources>
        <StyleSheet>
            <![CDATA[
            ^Button {
                text-align: left;
                color: black;
                position: absolute;
                background: #5dc1e4;
                font-size: 15;
                top: 0;
  
            }
            ]]>
        </StyleSheet>
    </ContentPage.Resources>

    <ContentPage.BindingContext>
        <local:MainPageViewModel/>
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout>

            <StackLayout HorizontalOptions="CenterAndExpand"  x:Name="NASForm" IsVisible="false">
                <Label Text="IP adresa" />
                <Entry x:Name="IPEntry" Text="{Binding IPNas}" />
                <Label Text="Korisničko ime" />
                <Entry x:Name="USEREntry" Text="{Binding UserNas}" />
                <Label Text="Lozinka" />
                <Entry x:Name="PASSEntry" Text="{Binding PassNas}" />
                <Label Text="Folder" />
                <Entry x:Name="FOLDEREntry" Text="{Binding Folder}" />
                <Label Text="Sub Folder"  />
                <Entry x:Name="SUBFOLDEREntry" Text="{Binding SubFolder}" />

                <Button Text="Save" Command="{Binding SaveCommandNAS}"  />
                <Button Text="Load" Command="{Binding LoadCommandNAS}"/>
                <Button Text="Delete"  Command="{Binding DeleteCommandNAS}"/>

            </StackLayout>

            <StackLayout HorizontalOptions="CenterAndExpand"  x:Name="MySQLForm" IsVisible="false">
                <Label Text="IP adresa" />
                <Entry x:Name="IPEntryMySQL"  Text="{Binding IP}" />
                <Label Text="Korisničko ime" />
                <Entry x:Name="USEREntryMySQL" Text="{Binding UserName}" />
                <Label Text="Lozinka" />
                <Entry x:Name="PASSEntryMySQL" Text="{Binding Password}"  />
                <Label Text="Ime baze podataka" />
                <Entry x:Name="databasenameEntryMySQL" Text="{Binding DatabaseName}" />

                <Button Text="Save"  Command="{Binding SaveCommand}" />
                <Button Text="Load"  Command="{Binding LoadCommand}" />
                <Button Text="Delete"  Command="{Binding DeleteCommand}" />

            </StackLayout>
            

            <!-->
            NAS FORMS
            -->
            <Image
                Source="logo.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" 
                BackgroundColor="White"
                />
            <Label
                Text="Dobrodošli"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />
            
            <Label
                Text="VPN server"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="NAS server"
                FontSize="18"
                HorizontalOptions="Center" />
            <Button
                x:Name="CounterBtn"
                StyleClass="my-button"
                Text="Spoji se"
                SemanticProperties.Hint="Spoji se"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center" >
                <!--<Button.Style>  !XML NAČIN DODAVANJA STILA!
                    <Style TargetType="Button">
                        <Setter Property="BackgroundColor" Value="#007acc" />
                        <Setter Property="TextColor" Value="White" />
                        
                        <Setter Property="Padding" Value="10, 20" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="FontAttributes" Value="Bold" />
                        
                        <Setter Property="TextTransform" Value="Uppercase" />
                    </Style>
                </Button.Style>-->
            </Button>
            <VerticalStackLayout HorizontalOptions="Center">
                <Button Text="NAS Postavke" Clicked="NASPostavkeClicked" />
                <Button Text="MySQL Postavke" Clicked="MySQLPostavkeClicked" />
            </VerticalStackLayout>
        </VerticalStackLayout>
        
    </ScrollView>
    
</ContentPage>
